# installer

MSIX/MSI via WiX, code-signed, Intune-ready, silent install, ADMX policy
template for enforced settings. Phase 6 work; empty in Phase 0.

Long-lead item to flag early even though it isn't Phase 0 work: procuring a
code-signing certificate typically takes weeks — start that process well
before Phase 6, not at the start of it.
