# UI 视觉稿预览

视觉稿文件位于仓库根目录：[`ui-design-preview.html`](./ui-design-preview.html)。

## 本地打开

1. 确认已切换到包含本文件的最新分支，并拉取最新提交。
2. 在项目根目录中找到 `ui-design-preview.html`（它与 `global.json`、`CLAUDE.md` 位于同一层）。
3. 双击该 HTML 文件，或右键选择浏览器打开；不需要运行 .NET、Node.js 或任何本地服务。

## 命令行打开（可选）

在项目根目录执行以下命令后，使用浏览器打开终端输出的完整路径：

```bash
pwd
```

预览稿是独立的静态 HTML 文件，所有样式都内置在文件中，不依赖网络资源或项目运行环境。
